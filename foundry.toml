[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc_version = "0.8.24"
optimizer = true
optimizer_runs = 10_000
remappings = ["@openzeppelin/=lib/openzeppelin-contracts/"]

[rpc_endpoints]
# Arc Testnet — Chain ID 5042002, native gas token is USDC (18 decimals at native layer)
arc_testnet = "https://rpc.testnet.arc.network"
